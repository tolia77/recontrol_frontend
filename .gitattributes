# Normalize all text files to LF in the repo and on checkout,
# so edits from Windows tooling can't reintroduce CRLF churn.
* text=auto eol=lf

# Binary assets — never touch line endings
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
