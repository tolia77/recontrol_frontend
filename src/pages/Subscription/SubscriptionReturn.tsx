import { LoadingState } from "src/components/ui";

// Placeholder — replaced by plan 04 with polling logic.
function SubscriptionReturn() {
  return <LoadingState />;
}

export default SubscriptionReturn;
